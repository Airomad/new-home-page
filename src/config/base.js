'use strict';

// Settings configured here will be merged into the final config object.
export default {
  WINDOW_WIDTH: window.innerWidth,
  WINDOW_HEIGHT: window.innerHeight,
  PX_SCALE_ARG: window.innerWidth / 1440
}
